# Get a key at https://platform.openai.com/api-keys
# EXPO_PUBLIC_ prefix is required for Expo to inline it into the bundle.
# That also means the key ships inside the app - acceptable for a personal
# app only. Never publish this app with a key that isn't spend-capped.
EXPO_PUBLIC_OPENAI_API_KEY=
